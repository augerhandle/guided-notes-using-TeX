\section{Some Polynomial Vocabulary}

\begin{center}
    \small
    \begin{tabular}{r@{\hskip 1em}l@{\hskip 2em}l@{\hskip 2em}l@{\hskip 1em}c}
        \toprule 
        {\scshape Type} & {\scshape Parent} & {\scshape Factored Form} & {\scshape Standard Form} & {\scshape  Constant Term} \\
        % {\itshape type} & {\itshape parent} & {\itshape factored form} & {\itshape standard form }\\
        \midrule 
        {\itshape quadratic} 
            & $f(x) = x^2$ 
            & $g(x)=(x-3)(2x+3) $
            & $g(x) = 2x^2 -3x - 9$ 
            & -9\\
        {\itshape cubic} 
            & $f(x) = x^3$ 
            & $g(x)=(x-1)(x+4)(3x+2) $
            & $g(x) = 3x^3 + 11x^2  - 6x - 8$ 
            & -8\\
        \bottomrule
    \end{tabular}
\end{center}

\myProblemsWithContent
{
    Write the \myEmph{quadratic parent function}.
    \tcblower
    $f(x) = $ \gap{$x^2$}
}
{
    Write the \myEmph{cubic parent function}.
    \tcblower
    $f(x) = $ \gap{$x^3$}
}

\myProblems
{
    Is this function in \myEmph{factored} or \myEmph{standard} form?
    $f(x) = (2x-3)(x+5)$
    \myAnswer{\hfill\tiny factored form}
}
{
    What is the \myEmph{constant term} in \\$f(x) = 2x^3 -3x^2 + 4x -5$?
    \myAnswer{\hfill\tiny -5}
}
{0.1in}