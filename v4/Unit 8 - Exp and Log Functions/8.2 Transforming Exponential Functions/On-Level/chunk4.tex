

\myProblems[%
    Find the base $b$, and find $a$, $h$, $k$.
    Then list the transformations involved in these functions.
]
{
    $g(x) = e^{x-3} -4$
}
{
    $g(x) = -2 \cdot 10^{x} -4$
}
{0.45in}

\myProblems[%
The base $b$ is given.
Find $a$, $h$, $k$.
Then write the equation for a transformed exponential functions involving the following transformations.
]
{
    $b=2$
    \begin{itemize}[nosep]
        \item shifted right by 5 
        \item shifted up by 2 
    \end{itemize}
}
{
    $b = 10$
    \begin{itemize}[nosep]
        \item reflected across the $x$-axis 
        \item stretched by a factor of $3$
        \item shifted left by 1 
        \item shifted down by 4 
    \end{itemize}
}
{1.5in}

\myProblems[%
Find the base $b$, and find $a$, $h$, $k$.
Then find these attributes of the given exponential functions
\begin{itemize}[nosep]
    \item Is it {\itshape growth} or {\itshape decay}?
    \item What is the equation of the {\itshape horizontal asymptote}?
    \item What are the {\itshape domain} and {\itshape range}?
\end{itemize}
]
{
    $g(x) = 10^{x-1} + 5$
}
{
    $g(x) = -5 \cdot 2^{x} - 3$
}
{2.5in}


       