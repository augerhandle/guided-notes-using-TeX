\dummypart{3}{Polynomials}
\myLesson{Factoring Polynomials with GCFs}[3][a]

\begin{myObjectives}
    \myObjective{find}{the factors of a monomial}
    \myObjective{find}{the greatest common factor of several monomials}
    \myObjective{factor}{polynomials by removing the GCF}
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{like terms}{monomials with the variable raised to the \gap{same} power}
    \myDefinition{factors}{things that are \gap{multiplied} together}
    \myDefinition{common factor}{a factor of \gap{several} terms}
    \myDefinition{greatest common factor}{the \gap{largest} common factor}
    \myDefinition{GCF}{\gap{greatest} \gap{common} \gap{factor}}
\end{myVocabulary}

\myCenteredBox[width=4.5in]{
    When we {\itshape factor something}, 
    we \gap{rewrite} it as several pieces 
    \gap{multiplied} together.
}


\input{chunk1}
\input{chunk2}
\input{chunk3}
