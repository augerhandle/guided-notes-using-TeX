


\begin{myConceptSteps}{To solve factored quadratic equations\dots}[
    \myCenteredBox[
        width=5in,
        coltitle=black,
        colbacktitle=blue!10!white,
        title={The Zero Product Property}
        ]
        {
        If several things multiplied together are \gap{zero}, 
        then at least one of them is \gap{zero}.
        }
        Use this property by following these steps.
    ]
    %
    \myStep{separate equations}{Set each factor equal to \gap{zero}.}
    \myStep{solve}{Solve these \gap{two} \gap{equations}.}
    \myStep{collect}{Write both solutions \gap{together} separated by a comma.}
\end{myConceptSteps}
