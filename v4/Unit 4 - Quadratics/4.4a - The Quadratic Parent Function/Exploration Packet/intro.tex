\section*{Introduction}

In this packet, you will explore \gap{transformations} of the quadratic parent function.

\begin{tcbraster}[
    raster columns = 2,
    raster left skip = 0.5in, raster right skip = 0.5in,
    raster before skip = 1em, raster after skip = 1em,
    raster equal height,
    valign = center,
]
    \begin{tcolorbox}[title={parent function}, colbacktitle=gray, valign=bottom,]
        \Large
        \vspace{-0.75em}
        \[ f(x) = x^2 \]    
    \end{tcolorbox}
    \begin{tcolorbox}[title={transformed function}, colbacktitle=gray, valign=bottom,]
        \Large
        \vspace{-0.75em}
        \[ g(x) = \bm{a}(x-\bm{h})^2 + \bm{k}\]    
    \end{tcolorbox}
\end{tcbraster}
%
% You will discover the \gap{effects} of $\bm{a}$, $\bm{h}$, and $\bm{k}$ on the parent function.