\dummypart{1}{Functions}
\chapter{Relations and Functions}

\begin{myObjectives}
    \myObjective{plot}{$(x,y)$ \gap{points} on a $x-y$ graph}
    \myObjective{find}{\gap{$y$}-values from a given \gap{$x$}-value given the graph of a relation}
    \myObjective{determine}{if a relation is a function using the \gap{vertical line} test}
\end{myObjectives}


• I can find the y-values from a given x-value given the graph of a relation.

\begin{myVocabulary}
    \myDefinition{plot points}{given the coordinates of a point, draw it as a \gap{dot} on a graph}
    \myDefinition{relation}{\gap{$x$}-values that are \gap{related} to \gap{$y$}-values}
    \myDefinition{function}{a relation where $x$-value are related to \gap{only one} $y$-value}
    \myDefinition{vertical}{\gap{up} and \gap{down}}
    \myDefinition{vertical line test}{a way to tell if a relation is a function using vertical lines}
\end{myVocabulary}


\input{chunk1}
\input{chunk2}
\input{chunk3}
