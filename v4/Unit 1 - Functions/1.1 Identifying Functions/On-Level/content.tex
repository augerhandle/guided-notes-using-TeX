\dummypart{1}{Functions}
\chapter{Identifying Functions}

\begin{myObjectives}
    \myObjective{determine}{if a relation is a function using the \gap{vertical line} test}
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{relation}{\gap{$x$}-values that are \gap{related} to \gap{$y$}-values}
    \myDefinition{function}{a relation where $x$-value are related to \gap{only one} $y$-value}
    \myDefinition{vertical}{\gap{up} and \gap{down}}
    \myDefinition{vertical line test}{a way to tell if a relation is a function using vertical lines}
\end{myVocabulary}


\input{chunk1}
\input{chunk2}
\input{chunk3}
