\section{Vertical Line Test}

\begin{myConceptSteps}{~to determine of a relation is a function\dots}
    \myStep{sketch}{Sketch the relation on an $x-y$ \gap{graph}. }
    \myStep{vertical lines}{Draw a \gap{vertical line} at every $x$-value in the relation. }
    \myStep{$y$-values}{Count the number of points each line intersectss}
    \myStep{test}{For functions, the lines must intersect \gap{only one} points}
\end{myConceptSteps}

\myProblem[In the following problems...]
    {do this}
    [\tiny]{2in}
\myWideProblem[In the following problems...]{do this}[\tiny]{2in}
\myProblems[In the following problems...]{do this}{222}[\large]{2in}

\begin{myWideProblemWithContent}{do this}
    \mySymmetricGraph[0.4]{8}[3]
\end{myWideProblemWithContent}

\begin{myWideProblemWithContent}{do this}
    \myNumberLine{8}[-8][$x$]
\end{myWideProblemWithContent}
