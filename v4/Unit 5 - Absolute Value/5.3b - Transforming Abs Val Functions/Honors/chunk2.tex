\section{The Transformations Due To $a$, $h$, $k$}


\subsection{Reflection---$a$}

The \gap{sign} of $\bm{a}$ determines whether the graph 
opens {\bfseries\itshape up} or {\bfseries\itshape down}:
\begin{itemize}[topsep=-0.5\parskip]
    \item If $\bm{a}>0$, the graph opens \gap{up}.
    \item If $\bm{a}<0$, the graph opens \gap{down}\dots (\gap{reflected} across the $x$-axis).
\end{itemize}


\subsection{Vertical Stretch/Compress---$a$}

The absolute value of $\bm{a}$ determines whether the graph 
has been vertically {\bfseries\itshape stretched} or {\bfseries\itshape compressed}.
\begin{itemize}[topsep=-0.5\parskip]
    \item If $|\bm{a}|>1$, the graph is \gap{narrower}\dotfill(\gap{vertical} \gap{stretch} by \gap{$|a|$}).
    \item If $|\bm{a}|<1$, the graph is \gap{wider}\dotfill(\gap{vertical} \gap{compression} by \gap{$|a|$}).
\end{itemize}

\subsection{Horizontal Shift---$h$}

\begin{tcolorbox}[center,width=4.75in,colback=white,toprule=0.5pt,leftrule=0.5pt,rightrule=0.5pt,bottomrule=0.5pt,]
    \centering\small
    Remember that $\bm{h}$ is the \gap{opposite} of what you see.
\end{tcolorbox}

The value of $\bm{h}$ determines whether the graph 
has been horizontally shifted {\bfseries\itshape right} or {\bfseries\itshape left}.
\begin{itemize}[topsep=-0.5\parskip]
    \item If $\bm{h}>0$, the graph is \gap{shifted} \gap{right} by \gap{$|h|$}.
    \item If $\bm{h}<0$, the graph is \gap{shifted} \gap{left} by \gap{$|h|$}.
\end{itemize}



\subsection{Vertical Shift---$k$}

The value of $\bm{k}$ determines whether the graph 
has been horizontally shifted {\bfseries\itshape up} or {\bfseries\itshape down}.
\begin{itemize}[topsep=-0.5\parskip]
    \item If $\bm{k}>0$, the graph is \gap{shifted} \gap{up} by \gap{$|k|$}.
    \item If $\bm{k}<0$, the graph is \gap{shifted} \gap{down} by \gap{$|k|$}.
\end{itemize}



\myProblemsWithContent[Describe the transformations that convert the parent function, $f(x)=|x|$, into the following functions.]
{
    $g(x) = |x+4| - 5$
    \tcblower
    ${a} =$ \gap{$1$},\quad ${h} =$ \gap{$-4$},\quad ${k} =$ \gap{$-5$}
    \vspace{1.25in}
}
{
    $g(x) = -\frac{1}{2}|x-7| + 1$
    \tcblower
    ${a} =$ \gap{$-1$},\quad ${h} =$ \gap{$7$},\quad ${k} =$ \gap{$1$}
    \vspace{1.25in}
}
