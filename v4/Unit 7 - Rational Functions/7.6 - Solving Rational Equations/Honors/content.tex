\dummypart{7}{Rational Functions}
\myLesson{Solving Rational Equations}[6]

\begin{myObjectives}
    \myObjective{solve.}{Solve equations that have rational expressions in them.}
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{rational}{something that involves a \gap{fraction}}
    \myDefinition{numerator}{\gap{top} of a fraction}
    \myDefinition{denominator}{\gap{bottom} of a fraction}
    \myDefinition{excluded values}{values that might lead to division by \gap{zero}}
    \myDefinition{clearing a fractions}{make the denominator \gap{go} \gap{away}}
\end{myVocabulary}

\input{chunk1}
