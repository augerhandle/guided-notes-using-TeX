% Do I want to print the text on top of the guided notes blanks?
\dashundergapssetup{teacher-mode=false,}

% ----------------------------------------------------------------
% Title, objectives, vocabulary
% ----------------------------------------------------------------
% My class "units" are based on the LaTeX \chapter macros. But we're not doing chapters
% in this one-lesson file. So we "fake out" the relevant 
% chapter (unit) number and title.
\renewcommand{\thechapter}{7} 
\renewcommand{\myCurrentChapterTitle}{Rational Functions}

\tagged{on-level}{\myLesson{Rational Function Asymptotes}[3][b]}
\tagged{pre-AP}{\myLesson{Rational Function Asymptotes and Graphing}[3][b]}

\begin{taggedblock}{on-level}
    \begin{myObjectives}
        \myObjective{identify}{the \gap{degree} and leading \gap{coefficient} of polynomials}
        \myObjective{find}{the \gap{horizontal} asymptotes of rational functions}
        \myObjective{find}{the \gap{vertical} asymptotes of rational functions}
    \end{myObjectives}
    \begin{myVocabulary}
        \myDefinition{degree}{the \gap{largest} exponent in a polynomial}
        \myDefinition{leading coefficient}{the number \gap{in front} of the variable with the largest exponent}
    \end{myVocabulary}    
\end{taggedblock}

\begin{taggedblock}{pre-AP}
    \begin{myObjectives}
        \myObjective{find}{the \gap{horizontal} asymptotes of rational functions}
        \myObjective{find}{the \gap{vertical} asymptotes of rational functions}
        \myObjective{sketch}{the graphs of rational functions with \gap{holes} }
    \end{myObjectives}
    \begin{myVocabulary}
        \myDefinition{degree}{the largest exponent in a polynomial}
        \myDefinition{leading coefficient}{the number in front of the variable with the largest exponent}
        \myDefinition{holes}{$(x,y)$ points where $x$ is \gap{excluded} from the domain}
    \end{myVocabulary}
\end{taggedblock}



            
% ----------------------------------------------------------------
% Now the real content
% ----------------------------------------------------------------
\input{./chunk-0-OL}
\input{./chunk-1}
\input{./chunk-2}
\input{./chunk-3}
