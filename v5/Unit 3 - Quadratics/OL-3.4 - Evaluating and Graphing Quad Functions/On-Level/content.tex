\dummypart{3}{Quadratic Functions}
\myLesson{Evaluating and Graphing Quadratic Functions}[4]

\begin{myObjectives}
    \myObjective{evaluate}{evaluate quadratic functions for a given \gap{input} value}
    \myObjective{sketch}{%
        the graph of quadratic functions
        \begin{itemize}
            \item sketch the curve
            \item draw a \myEmph{dot} at the \gap{vertex}
            \item draw the \gap{axis} \gap{of} \gap{symmetry} (AOS)
        \end{itemize}
    }
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{quadratic}{degree 2 (largest exponent is 2)}
    \myDefinition{quadratic function}{\gap{$f(x) = ax^2 + bx + c $}}
    \myDefinition{parabola}{the \gap{shape} of the graph of a quadratic function}
    \myDefinition{vertex}{the \gap{highest} or \gap{lowest} point on a parabola}
    \myDefinition{symmetric}{\gap{mirror} images}
\end{myVocabulary}


\input{chunk1}
\input{chunk2}
