\dummypart{4}{Rational Functions}
\myLesson{Vertical Asymptotes and Holes}[6][a]

\begin{myObjectives}
    \myObjective{find}{the \myEmph{vertical asymptotes} of a rational function.}
    \myObjective{find}{the \myEmph{holes} of a rational function.}
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{rational function}{a \gap{fraction} of polynomials}
    \myDefinition{numerator}{\gap{top} of a fraction}
    \myDefinition{denominator}{\gap{bottom} of a fration}
    \myDefinition{excluded values}{$x$ values that would make us divide by \gap{zero}}
    \myDefinition{vertical asymptote}{a vertical line that a graph approaches but \gap{never} reaches}
    \myDefinition{holes}{points on a graph that come from \gap{cancelled} \gap{factors}}
\end{myVocabulary}


\input{chunk1}
\input{chunk2}
\input{chunk3}
