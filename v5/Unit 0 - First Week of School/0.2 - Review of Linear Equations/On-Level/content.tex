\dummypart{1}{Systems of Linear Equations}
\myLesson{Review of Linear Equations}[1]

\begin{myObjectives}
    \myObjective{rewrite}{linear equations in {slope-intercept} form}
    \myObjective{sketch}{the graph of linear equations}
\end{myObjectives}
            
\begin{myVocabulary}
    \myDefinition{linear equation}{an equation with variables only to the \gap{first} power}
    \myDefinition{slope-intercept form}{\gap{$y=mx+b$}}
    \myDefinition{standard form}{\gap{$ax+by=c$}}
    \myDefinition{isolate}{to get something \gap{by} \gap{itself}}
    \myDefinition{slope}{\gap{rise} over \gap{run}}
    \myDefinition{$y$-intercept}{where the line goes through the \gap{y-axis}}
    \myDefinition{horizontal}{left/right}
    \myDefinition{vertical}{up/down}
\end{myVocabulary}

\input{chunk1}
\input{chunk2}
