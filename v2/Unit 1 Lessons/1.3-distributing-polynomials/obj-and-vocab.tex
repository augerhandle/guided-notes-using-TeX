\begin{myObjectives}
    \myObjective{multiply}{polynomials by distributing}
\end{myObjectives}

\begin{myVocabulary}
    \myDefinition{mono-}{one}
    \myDefinition{bi-}{two}
    \myDefinition{poly-}{many}
    \myDefinition{term}{numbers and variables multiplied or divided together}
    \myDefinition{coefficient}{
        the number in front of a term, 
        eg, the coefficient of $5x^2$ is 5
    }
    \myDefinition{monomial}{a single term by itself, eg, $5x^2$}
    \myDefinition{binomial}{two terms added or subtracted, eg, $8x-5$}
    \myDefinition{polynomial}{many terms added or subtracted, eg, $2x^2 - 3x + 5$}
\end{myVocabulary}


